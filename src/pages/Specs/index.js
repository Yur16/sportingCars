import React from 'react';
import { Text } from 'react-native';

import { Container } from './styles';

export default function Specs(){
  return(
    <Container>
      <Text style={{ color: '#fff' }}>Especificações dos carros</Text>
    </Container>
  );
}